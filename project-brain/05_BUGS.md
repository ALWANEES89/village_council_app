# سجل الأخطاء

## موانع الجاهزية الإنتاجية — غير مصنفة بعد برقم BUG/TASK

- الحالة: **Code-remediated / Operational verification open**.
- الأولوية: Critical/High.
- تاريخ المراجعة: 2026-07-23.
- أُغلقت محليًا إساءة إنشاء الإشعارات، قراءة `financial_profile` العابرة للمجالس،
  عدم اتساق المالك الأساسي، الكتابة العميلية للحجوزات وغياب slot lock، seed
  التلقائي، غياب kill switches، وfallback توقيع Release إلى debug.
- بقيت موانع لا يمكن إغلاقها في هذه الجولة: جرد/توافق بيانات production، App
  Check metrics/enforcement في Console، Scheduler/Pub/Sub E2E على staging
  منشور، نسخة احتياطية واختبار restore، وAndroid Release موقعة بمفتاح حقيقي.
- الأدلة: Flutter 51/51، Functions 22/22، Emulator 34/34، JSON 5/5،
  `node --check` ‏10/10، وAPK compile غير موقع. التفاصيل والعقود في
  `PRE_PRODUCTION_READINESS_REPORT_AR.md`.
- لم يُنشأ رقم جديد حتى يعتمد تصنيف Project Brain؛ لا يغير ذلك اكتمال
  `BUG-002` أو MAJ-8 أو `TASK-004`.

## قالب خطأ
### BUG-000 — عنوان الخطأ
- الحالة: Open
- الأولوية:
- تاريخ الاكتشاف:
- الوصف:
- السبب الجذري:
- الملفات المتأثرة:
- الحل:
- الاختبارات:
- تاريخ الإغلاق:

### BUG-009 — ثغرات فصل وصلاحيات ظهرت في QA النهائي للـDashboard
- الحالة: Closed locally / Physical-device QA pending.
- الأولوية: High.
- تاريخ الاكتشاف: 2026-09-06.
- الوصف: كانت شاشة إنشاء المجلس تعرض النموذج عند الوصول المباشر لغير مالك
  النظام رغم منع الحفظ، وكان قرار دخول النظام يمر عبر مزود يتابع سياق المجلس.
  كما كانت لوحة المجلس تقرأ `fullAccess` مباشرة من snapshot، وكان بحث مبدل
  المجالس يطابق الاسم المعروض بلغة واحدة فقط.
- السبب الجذري: حماية الحفظ لم يصاحبها Guard كامل للعرض، وإعادة استخدام مزود
  صلاحيات المجلس لقرار منصة عالمي، وتكرار منطق `fullAccess` خارج `AdminAccess`.
- الملفات المتأثرة: `app_providers.dart`، لوحتا المجلس والنظام،
  `create_organization_screen.dart`، `organizations_management_screen.dart`،
  واختبارات Dashboard واللغة.
- الحل: مزود عالمي مستقل للمجلس، Guards على مستوى الشاشات، تمرير قرار الوصول
  الكامل عبر `AdminAccess`، وبحث متعدد حقول الاسم مع الحفاظ على قيد العضويات.
- الاختبارات: موجهة 26/26، Flutter كاملة 85/85، `flutter analyze` نظيف، وبناء
  APK Debug ناجح. لم يتوفر هاتف متصل عبر ADB.
- تاريخ الإغلاق: 2026-09-06 محليًا.

### BUG-001 — تعذر إنشاء Signed URL للإيصال داخل Functions Emulator
- الحالة: Closed / Device verified
- الأولوية: High
- تاريخ الاكتشاف: 2026-07-16
- الوصف: فشل فتح PDF من شاشة المراجع برسالة `firebase_functions/internal` داخل Emulator.
- السبب الجذري: بيئة Emulator لا تحتوي service-account `client_email` اللازم لتوقيع Signed URL.
- الملفات المتأثرة: `functions/financial.js`، `lib/data/repositories/financial_repository.dart`، `lib/presentation/screens/admin/financial_review_screen.dart`.
- الحل: bytes خاصة بالـEmulator بعد التفويض الصارم، مع بقاء Signed URL للإنتاج.
- الاختبارات: فتح PDF بصريًا على Samsung من حساب المراجع، و19 اختبار Emulator، واختبارات صاحب الإيصال والمراجع ذي `membershipId` المختلف والعزل بين المجالس والملفات غير الصالحة.
- تاريخ الإغلاق: 2026-07-16.

### BUG-002 — تقويم توفر الحجوزات يفشل لحساب العضو على Android
- الحالة: Closed / Device verified
- الأولوية: High
- تاريخ الاكتشاف: 2026-07-23
- الوصف: تعرض شاشة «حجز المجلس» للحساب ذي العضوية رسالة «تعذر تحميل مواعيد الحجز» بدل التقويم، مع بقاء قائمة حجوزات العضو ظاهرة. تكرر ذلك مرتين على Samsung `SM-S948B`. الحساب الضيف على الجهاز نفسه عرض التقويم والحجز المعتمد بنجاح.
- السبب الجذري: `CouncilBookingScreen` يستخدم `organizationBookingsProvider` عندما توجد `membershipId`، وهذا ينفذ استعلامًا غير مقيّد على جميع حجوزات المجلس. قواعد Firestore تسمح للعضو بقائمة مقيدة بحجوزاته فقط، وتخصص `getBookingAvailability` لعرض التوفر المنقح؛ لذلك يُرفض استعلام العضو كما هو مقصود أمنيًا.
- الملفات المتأثرة: `lib/presentation/screens/member/council_booking_screen.dart`، `lib/providers/app_providers.dart`، `lib/data/repositories/booking_repository.dart`، ومرجع السياسة في `firestore.rules`.
- الحل: أصبح تقويم العضو والضيف يستخدم `bookingAvailabilityProvider`/`getBookingAvailability` دائمًا. بقي `userBookingsProvider` المقيد بـ`userId` لقائمة «حجوزاتي» فقط. لم تتغير Firestore Rules ولم تُمنح العضوية صلاحية list غير مقيدة. أضيف parser منقح يتجاهل أي حقول زائدة ويحوّل `date` و`status` فقط إلى نموذج عرض التقويم.
- الاختبارات: Flutter ‏46/46، Functions ‏17/17 على Node `v20.20.2`، Firebase Emulator ‏23/23 على `demo-financial-prestaging`. تغطي الاختبارات نجاح العضو والضيف، التنقيح إلى `date/status`، عزل المجالس، بقاء list العضو مقيدًا، وحالات loading/empty/error. إعادة Samsung نجحت 2/2؛ ظهر التقويم و«حجوزاتي» وظهر يوم الحجز المعتمد غير متاح دون بيانات شخصية.
- تاريخ الإغلاق: 2026-07-23.

### BUG-003 — تعذر الوصول المستقر إلى إعداد رسوم حجز الأعضاء
- الحالة: Closed locally / Not deployed.
- الأولوية: High.
- تاريخ الاكتشاف: 2026-09-03.
- الوصف: قد تختفي إعدادات رسوم الحجز بالكامل عند تعذر تحميل باقات الاشتراك، وقد
  لا تظهر للمدير المالي ذي `permissionsSnapshot` القديم رغم أن دوره مخول خادميًا.
- السبب الجذري: تبويب الإعدادات كله كان متداخلًا تحت حالة تحميل الباقات، وفحص
  الواجهة لم يطابق دور `financialManager` صراحةً وكان يسمح خطأً بصلاحية
  `receipts.review` وحدها. كما كان فشل غياب مستخدم Auth يؤدي إلى عدم تنفيذ الحفظ
  بصمت، ولم يتحقق callable داخل المعاملة من وجود وثيقة المجلس.
- الملفات المتأثرة: `financial_management_screen.dart`، `admin_dashboard.dart`،
  `council_dashboard_screen.dart`، `admin_access.dart`، `financial_repository.dart`،
  و`functions/financial.js`.
- الحل: فصل بطاقة رسوم الحجز عن تحميل الباقات، توحيد قدرة
  `canManageFinancialSettings`، وإضافة مدخل الإدارة المالية داخل المجلس نفسه،
  وإظهار نتيجة الحفظ، والتحقق الخادمي من المعرف والعضوية والمجلس والقيمة الصحيحة
  بالبيسة. بقيت Rules تمنع الكتابة العميلية.
- الاختبارات: `flutter analyze` نظيف، Flutter ‏57/57، Functions ‏22/22، Emulator
  ‏38/38 على Node `v20.20.2`، ويتضمن اختبارًا يحفظ `7250` بيسة ثم يعتمد حجز
  عضو ويتحقق من الرسم.
- تاريخ الإغلاق: 2026-09-03 محليًا؛ لم يُنشر إلى الإنتاج.

### BUG-004 — شاشة حمراء وفشل callables أثناء QA المالي على Android
- الحالة: Closed locally / Device verified / Not deployed.
- الأولوية: High.
- تاريخ الاكتشاف: 2026-09-03.
- الوصف: ظهر `Unauthenticated` في الحجز ورفع الإيصال داخل Emulator رغم أن Auth
  صالح، ثم ظهرت شاشة Flutter حمراء بعد حفظ إعداد الاشتراك.
- السبب الجذري: كان App Check مفروضًا داخل Functions Emulator الذي لا يملك رمز
  App Check مسجلًا للهاتف، وكانت نوافذ الإدارة المالية تتخلص من
  `TextEditingController` قبل انتهاء حركة إغلاق `DialogRoute`.
- الملفات المتأثرة: `functions/production_security.js`، `functions/financial.js`،
  `financial_management_screen.dart`، و`firebase_emulator_config.dart`.
- الحل: بقي App Check مفروضًا في الإنتاج وعُطل فقط عند
  `FUNCTIONS_EMULATOR=true`، وأصبحت حقول نوافذ الاشتراك والحجز والباقات والحسابات
  والرسم اليدوي تستخدم قيم نموذج يملكها مسار الحوار دون التخلص المبكر. جُعلت
  تهيئة Firebase قابلة لإعادة الاستدعاء في فحص Debug.
- الاختبارات: نجح على Samsung فتح الصفحة وحفظ `13.750` وإعادة فتحها، وثبتت قراءة
  Firestore القيمة `13750` كـ `integerValue`. نجح رفع الإيصال وتوفر الحجز، وكان
  سجل Flutter خاليًا من الأخطاء بعد تثبيت الإصلاح الكامل. اختبارات الانحدار
  Flutter ‏6/6 ضمن الإجمالي ‏57/57، وFunctions ‏22/22، وEmulator ‏38/38.
- تاريخ الإغلاق: 2026-09-03 محليًا؛ لم يُنشر إلى الإنتاج.

### BUG-005 — مجموعة Cloud Functions الإنتاجية لا تطابق النظام المالي المحلي
- الحالة: Open / Release blocker.
- الأولوية: Critical.
- تاريخ الاكتشاف: 2026-09-05 أثناء مراجعة عمل كوبايلوت.
- الوصف: أظهر جرد `firebase functions:list` وجود 17 دالة فقط في الإنتاج، بينما
  يصدّر الكود المحلي دوال مالية وتشغيلية إضافية يعتمد عليها التطبيق، ومنها
  `submitFinancialReceipt` و`reviewFinancialReceipt` و`listFinancialMembers`
  و`updateMemberFinancialAccount` و`getGuestBookingCharge`.
- تأكيد الجهاز: في 2026-09-05 عرضت شاشة «حسابات الأعضاء» رسالة «تعذر تحميل
  دليل الأعضاء»، وفشل البحث عند الدفع عن الآخرين؛ السجل الحي أكد
  `FirebaseFunctionsException`، والجرد أكد غياب `listFinancialMembers` و
  `searchCouncilMembers` من الإنتاج رغم نجاحهما محليًا وعلى Emulator.
- السبب الجذري: نُشرت مجموعة جزئية يدويًا أثناء جلسة كوبايلوت بدل تنفيذ خطة
  إصدار موحدة ومراجعة قائمة الدوال وخطة التراجع.
- الملفات المتأثرة: `functions/index.js` و`functions/financial.js` وبيئة Firebase
  الإنتاجية. لا توجد معالجة إنتاجية منفذة في مراجعة 2026-09-05.
- الحل المطلوب: جرد الفرق كاملًا، اعتماد خطة نشر/تراجع، الاختبار على Staging،
  ثم طلب إذن صريح منفصل قبل أي تغيير إنتاجي.
- الاختبارات: الجرد الإنتاجي كان قراءة فقط. البوابات المحلية خضراء: Flutter
  ‏62/62، Functions ‏23/23، Emulator ‏40/40. لم يحدث نشر أو تعديل إنتاج.

### BUG-006 — مراجع الحجز لا يرى التصنيف الذي يحدد الرسم
- الحالة: Closed locally / Not deployed.
- الأولوية: High.
- تاريخ الاكتشاف: 2026-09-05 أثناء مراجعة عمل كوبايلوت.
- الوصف: يختار مقدم الطلب `regular` أو`event`، ويستخدمه الخادم لاختيار الرسم،
  لكن شاشة مراجعة الحجز كانت تعرض المناسبة فقط دون عرض التصنيف المالي الصريح.
- السبب الجذري: أضيف الحقل لمسار الإنشاء والحساب دون إضافته إلى بطاقة المراجعة.
- الملفات المتأثرة: `booking_requests_review_screen.dart` و
  `financial_emulator.test.js`.
- الحل: عرض «نوع الحجز: عادي/مناسبة» للمراجع، وإضافة اختبار Emulator يثبت حفظ
  `event` عبر `createBooking` ورفض التصنيف غير المعروف قبل إنشاء المستند.
- الاختبارات: ضمن Emulator ‏40/40 على Node 20، مع نجاح التحليل والاختبارات الكاملة.
- تاريخ الإغلاق: 2026-09-05 محليًا؛ قرار من يملك تغيير التصنيف موثق في `TASK-008`.

### BUG-007 — دور العضو العادي يرث صلاحية كاملة من قالب ملوث
- الحالة: Closed locally / Production data repair pending.
- الأولوية: Critical.
- تاريخ الاكتشاف: 2026-09-05 أثناء QA Android لعضو رقم 003.
- الوصف: تغيير دور العضو إلى `member` يعرض نجاح العملية، لكن الواجهة تستمر في
  تصنيفه «مدير (صلاحيات كاملة)».
- السبب الجذري: مستند الإنتاج `organizations/{organizationId}/roles/member`
  يحتوي `fullAccess`، وكان العميل ينسخ صلاحيات القالب كاملة إلى
  `permissionsSnapshot`. كما كان يخرج مبكرًا إذا لم يتغير `roleId`، فلا ينظف
  snapshot الملوث.
- الحل: تنقية `fullAccess` من دور العضو في جميع مسارات العميل، منع اختيارها أو
  حفظها في شاشة/مستودع الأدوار، وإضافة Firestore Rules تمنع كتابة قالب أو عضوية
  `member` تحمل `fullAccess`. بقي إصلاح المستند الموجود في الإنتاج ضمن
  `TASK-011` ويتطلب موافقة صريحة.
- الاختبارات: اختبارا Dart لسياسة التنقية واختبار Emulator يمنعان التصعيد حتى
  من مالك المجلس ومالك المنصة؛ الإجمالي Flutter ‏68/68 وEmulator ‏41/41.

### BUG-008 — إيصال صفري يظهر مطابقًا وقابلًا للاعتماد
- الحالة: Closed locally / Not deployed.
- الأولوية: High.
- تاريخ الاكتشاف: 2026-09-05 أثناء QA Android لشاشة مراجعة الإيصالات.
- الوصف: إيصال بمبلغ صفري ومجموع توزيعات صفري ظهر «مطابقًا» مع زر اعتماد فعال.
- السبب الجذري: الواجهة اعتمدت على مساواة المبلغين فقط، و`0 == 0` صحيح، دون
  اشتراط مبلغ موجب ووجود توزيع وحالة مراجعة معلقة.
- الحل: توحيد قرار الزر والتنفيذ مع `canApproveReceipt` وإضافة شرط وجود توزيع،
  وإظهار «بيانات الإيصال غير مكتملة» مع إبقاء الرفض متاحًا لتنظيف السجل.
- الاختبارات: اختبار انحدار لقيمة صفرية، والتحليل والاختبارات الكاملة خضراء.
- تاريخ الإغلاق: 2026-09-05 محليًا.
### BUG-009 — غياب مسار الانضمام وتعارض صلاحية تنبيه المجلس

- الحالة: Fixed locally / final Android send click pending.
- الأولوية: High.
- السبب الجذري: صفحة البداية لم تكن تحول الحساب بلا عضوية لمسار الانضمام، كما أن
  واجهة التنبيه قبلت `announcements.manage` بينما الخادم لم يعترف بها. واستعلام
  طلبات المستخدم عبر collection-group لم تكن له قاعدة list مطابقة.
- الحل: توجيه واعٍ بحالة العضويات، شاشة بحث موحدة، منع طلب مكرر، قاعدة قراءة
  ذاتية مقيدة، وتوحيد capability بين UI والدالة مع تحقق tenant لحظة الإرسال.
- الاختبارات: Flutter 102/102، Node 28/28، واختبارات Emulator الأمنية الموجهة 2/2.
