# تقرير جولة QA المالية والحجوزات غير المراقبة — حتى 6 ساعات

**بدأت الجولة:** 17 يوليو 2026، 01:31 بتوقيت مسقط
**بيئة التنفيذ الوحيدة:** `demo-financial-prestaging` عبر Firebase Emulator
**الجهاز المستهدف:** Samsung `SM-S948B`
**HEAD عند البداية:** `72b3428`
**Node:** `v20.20.2`
**الحالة:** أُغلقت البوابات الآلية في 22 يوليو، ثم أُغلق `BUG-002` بتحقق Samsung مرتين في 23 يوليو 2026؛ MAJ-8 و`TASK-004` مكتملتان 100% ضمن نطاق المرحلة.

## 1. الملخص التنفيذي المرحلي

- اكتملت حماية baseline مع الحفاظ الكامل على تغييرات QA والعملات السابقة.
- لا توجد ملفات staged، ولم يُنفذ commit أو push أو deploy أو migration.
- قُسم العمل إلى أربعة مسارات غير متداخلة: عقود JSON، الحجوزات والإلغاء والضيف، تدقيق العملة والأمان، والهاتف والدمج النهائي.
- أضيف حارس إلى سكربت تحقق QA يرفض `alrahmat-console` ويرفض أي project ID لا يبدأ بـ`demo-`. سكربت seed الموجود كان أصلًا أشد تقييدًا ويقبل `demo-financial-prestaging` فقط مع منافذ Emulator على loopback.

## 2. Baseline وحماية الحالة

### حالة Git عند البدء

- 38 مدخلًا معدلًا/غير متتبع في `git status --short`، منها 9 ملفات غير متتبعة فعلية وفق `git ls-files --others --exclude-standard`.
- آخر commit: `72b3428 feat(finance): complete secure pre-staging workflow`.
- لا توجد تغييرات staged.
- التغييرات الموجودة تشمل إصلاح فتح الإيصال، QA Emulator، توحيد الريال العُماني، الاختبارات والتوثيق. عوملت كلها كتغييرات مستخدم محفوظة.
- `.gradle/9.3.0/fileHashes/fileHashes.lock` كان معدلًا محليًا قبل الجولة، حجمه 17 بايت وآخر تعديل 15 يوليو 2026 15:28؛ لن يُلمس أو يُنظف أو يُجهز.

### Baseline الاختبارات الموثق من الجولة السابقة

| البوابة | آخر نتيجة مكتملة قبل بدء الجولة |
|---|---:|
| `npm test` | 16/16 ناجح |
| Firebase Emulator | 19/19 ناجح |
| `flutter test` | 32/32 ناجح |
| `flutter analyze` | 0 مشكلة |
| `node --check` | 4/4 ناجح |
| JSON | 19 صالحًا، و3 ملفات صفرية غير صالحة قيد التحقيق |
| الأسرار | 0 تطابق عالي الثقة |
| `git diff --check` | ناجح |

لن تُستخدم هذه النتائج بدل البوابات النهائية إذا تغيرت الملفات ذات الصلة.

## 3. خطة التنفيذ وحالة المراحل

| المرحلة | الحالة | المالك |
|---|---|---|
| حماية الحالة والحراس | مكتملة | الوكيل الرئيسي |
| عقود JSON | مكتملة مع استثناءات legacy موثقة | Agent A |
| الحجوزات والضيف والإلغاء | قيد الاختبار | Agent B |
| العملة والإشعارات والأمان | قيد التدقيق | Agent C |
| Android system tray والهاتف | لم يبدأ | الوكيل الرئيسي |
| البوابات النهائية وProject Brain | لم يبدأ | الوكيل الرئيسي |

## 4. قواعد السلامة المطبقة

- المشروع الافتراضي في `.firebaserc` هو production، لذلك لا يُستخدم أي أمر Firebase بلا `--project demo-financial-prestaging` صريح.
- ممنوع `firebase deploy` وmigration وcommit وpush وGit destructive commands.
- لا تُستخدم بيانات شخصية أو إيصالات حقيقية.
- `onlinePaymentsEnabled=false` وApp Check غير مفعلين.
- أي قرار غامض في مخطط البيانات يُوثق بدل التخمين.

## 5. النتائج المرحلية

### المرحلة الأولى — حماية الحالة والحراس

- تحقق Node الفعلي: `v20.20.2`.
- نجح اختبار الرفض المبكر لـ`alrahmat-console` في seed جهاز QA.
- نجح اختبار رفض `alrahmat-console` و`staging-financial` في `validate-financial-staging.ps1` قبل أي اتصال أو كتابة.
- أُوقفت جلسة `flutter run` القديمة بأمان لتحرير قفل Flutter مع بقاء التطبيق على الهاتف؛ لم تُمسح بياناته.
- سُجل SHA-256 لملف Gradle المستبعد: `92F825A6F42A9FA77A1349EC2A2D0ABB24E904C00D1C9555B6D2983CC2C33C8E`، ولن يُسمح للبوابات بتغيير مساره.

### المرحلة الثانية — تحقيق JSON

- أثبت Git history أن `dashboard.json` و`project.json` و`versions.json` أُدخلت فارغة فقط في commit `82f1cd9510cd558073756d49a04fd28750238e9b` بتاريخ 11 يوليو 2026، وبقيت صفرية في `origin/main` ونسخة OneDrive.
- لم يوجد لها reader أو writer أو reference في أي revision. العقد الفعلي الحالي هو `tasks.json` ← `update-project-dashboard.ps1` ← `PROJECT_DASHBOARD.md`، والعقد القديم المنفصل هو `tasks.csv` مع `project_status.json`.
- التصنيف: **unused legacy placeholders** وليست configuration أو generated outputs.
- لم تُحذف ولم تُملأ بـ`{}` أو`[]` بسبب منع حذف ملفات المستخدم وغياب عقد يمكن إثباته.
- أُضيف validator مع allowlist دقيقة لهذه الملفات الثلاثة فقط؛ يرفض أي JSON متتبع آخر فارغًا أو مفقودًا أو غير صالح.
- نتائج validator: `node --check` ‏2/2، واختباراته 5/5، وبوابة المستودع 15 JSON صالحًا و3 استثناءات legacy معلنة، 0 خطأ.

## 6. الوقت الفعلي المستغرق

يُحسب عند إغلاق الجولة من وقت البدء أعلاه حتى آخر بوابة أو عائق آمن.

## 7. تأكيدات مؤقتة

- لم يُستخدم `alrahmat-console`.
- لم يُنفذ deploy أو migration أو push أو commit.
- لم يُلمس ملف Gradle المحلي.

## 8. نتيجة الإغلاق الآلي النهائية — 22 يوليو 2026

- حُفظت جميع التغييرات المحلية فوق `72b3428`، ولم تُستخدم أوامر Git مدمرة ولم يُنشأ commit أو push.
- شُغلت Functions وFirebase Emulator على Node `v20.20.2` الرسمي المحمول، وأكد Emulator: `Using node@20 from host`.
- نجحت Functions ‏17/17، وFirebase Emulator ‏23/23، وFlutter ‏40/40، واختبارات JSON ‏5/5، و`node --check` ‏10/10.
- نجح اختبار حجز عضو متصل جديد يغطي الإنشاء، الاعتماد والصلاحيات، الرسم، رفع الإيصال، الرفض، السداد الجزئي والكامل، الإلغاء و`refundRequired` ومنع التكرار.
- نجحت تغطية الضيف الحالية لرسم غير المشترك والخصوصية والسداد الجزئي والكامل ومنع الدفع الزائد.
- أُصلحت 4 من أصل 8 قضايا تحليل آمنة بلا تغيير سلوك. بقيت 4 تحذيرات `unused_element` قديمة في `admin_dashboard.dart`.
- بوابة JSON تقبل 15 ملفًا صالحًا وتستثني الملفات الثلاثة الفارغة legacy بالـallowlist الموثقة فقط؛ لم تُملأ بقيم تخمينية.
- فحص 56 ملفًا نصيًا معدلًا/جديدًا: 0 سر أو مفتاح أو IBAN عالي الثقة، و0 اسم ملف حساس.
- لا يوجد جهاز Android متصل عبر ADB، لذلك لم يُنفذ اختبار system tray أو إعادة QA البصري الكامل للحجز على Samsung.

**الحكم:** الإغلاق الآلي ناجح، لكن MAJ-8 و`TASK-004` تبقيان `in_progress` وليستا 100% حتى إتمام QA الهاتف اليدوي والمتطلبات اللاحقة الموثقة.

## 9. نتيجة QA الهاتف الفعلية — 23 يوليو 2026

- الجهاز: Samsung `SM-S948B` مع APK Debug موجه حصريًا إلى `demo-financial-prestaging` عبر Emulator وADB reverse.
- Android system tray: **4/4 ناجح بصريًا** للقيم `5.000` و`7.500` و`8.000` و`12.500 ر.ع.`. لا «بيسة» ولا أرقام بيسة خام. الاختبار استخدم مستقبل QA المحلي الخاص بـDebug، وليس FCM إنتاجيًا.
- شاشة العضو المالية: **ناجحة بصريًا** لرسم الحجز `5.000`، الرصيد الجزئي `7.500`، والاشتراك `12.500` مع الرمز الرسمي وثلاث خانات.
- شاشة الضيف: **ناجحة بصريًا**؛ ظهر التقويم والحجز المعتمد، وظهرت رسوم الحجز `7.500` وحالة `unpaid` ومسار اختيار/رفع الإيصال.
- شاشة حجز العضو: ظهر الحجز المعتمد، لكن تقويم التوفر فشل **2/2**. السبب هو list غير مقيد لجميع حجوزات المجلس عبر `organizationBookingsProvider`، وهو مرفوض عمدًا بقواعد Firestore؛ سجل كـ`BUG-002`. لا يوجد مبرر لتوسيع Rules، والحل المقترح هو استعمال callable التوفر المنقح للعضو أيضًا.
- ملاحظة إعداد: جلسة Firebase محلية قديمة أعادت `INVALID_REFRESH_TOKEN`؛ مُسحت بيانات التطبيق المحلية فقط ثم سُجل الدخول بحسابات Emulator وهمية. لم تُمس بيانات Firebase أو production.
- لا commit أو push أو deploy أو migration، ولم يُستخدم `alrahmat-console`.

**الحكم النهائي:** اختبارات العرض المطلوبة نجحت، لكن MAJ-8 تبقى أقل من 100% حتى إصلاح `BUG-002` وإعادة تقويم العضو على الهاتف. تبقى `TASK-004` `in_progress` عند 98%.

## 10. إغلاق BUG-002 وإعادة QA العضو — 23 يوليو 2026

- السبب الجذري: شرط الواجهة كان يختار `organizationBookingsProvider` للعضو، فيصطدم عمدًا بمنع list جميع حجوزات المجلس في Rules.
- الحل المحدود: تقويم العضو والضيف يستخدمان `bookingAvailabilityProvider` وcallable `getBookingAvailability`. بقيت «حجوزاتي» على `userBookingsProvider` المقيد بـ`organizationId` و`userId`.
- الخصوصية: لم تُعدّل Rules. لا تعيد الدالة إلا `date` و`status`، وأثبتت الاختبارات تجاهل الاسم والهاتف والملاحظات والإيصال وحقول الرسم، وعدم إدخال حجز من مجلس آخر.
- التغطية: حالات loading وerror وempty واليوم المعتمد غير القابل للاختيار في Flutter، نجاح العضو والضيف والتنقيح والعزل في Emulator، وبقاء استعلام «حجوزاتي» مقيدًا.
- النتائج: Flutter ‏46/46، Functions ‏17/17 على Node `v20.20.2`، Firebase Emulator ‏23/23، `flutter analyze` أربع تحذيرات قديمة و0 أخطاء جديدة، و`git diff --check` ناجح.
- Samsung: نجح فتح تقويم العضو مرتين 2/2 دون رسالة التعذر، واستمرت «حجوزاتي» في عرض الحجز المعتمد. ظهر يوم 6 أغسطس كغير متاح.

**الحكم المحدث:** `BUG-002` مغلق بتحقق الجهاز، وMAJ-8 جاهزة 100%، و`TASK-004` `done` بنسبة 100%. لم يحدث commit أو push أو deploy أو migration، ولم يُستخدم production.
