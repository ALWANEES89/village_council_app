# سجل التحديثات

## 2026-07-23 — معالجة موانع الجاهزية CRITICAL/HIGH محليًا

- نقل إنشاء الإشعارات والحجوزات وBootstrap/repair إلى Cloud Functions محمية،
  وإغلاق الكتابة العميلية ومسارات FCM غير الموثوقة في Rules والتطبيق.
- عزل `financial_profile` حسب المجلس والصلاحية، وتوحيد المالك الأساسي مع إسقاط
  `member_access/{userId}` ودعم legacy آمن دون افتراض تطابق معرف العضوية.
- إضافة slot locks ذرية للحجوزات، اختبار تزامن بفائز واحد، دعم حجز legacy بلا
  lock، pagination للتوفر دون كشف بيانات المالك، وتحرير القفل المقيد بالحجز.
- منع seed التلقائي في التشغيل العادي، وإضافة App Check لـAndroid، kill switches
  آمنة للمجدولات، وأدوات inventory/migration manifest/rollback دون تشغيل إنتاجي.
- إزالة debug-signing fallback وإضافة إعداد توقيع محلي مستبعد. نجح compile إلى
  APK غير موقع؛ لا يوجد keystore إنتاج، لذلك لا تعد هذه نسخة نشر.
- البوابات: Flutter 51/51، Functions 22/22 على Node 20.20.2، Emulator 34/34 على
  `demo-financial-prestaging`، JSON 5/5، `node --check` ‏10/10، وصفر أخطاء analyze
  مع بقاء 4 تحذيرات Widgets القديمة.
- القرار المحدث ما زال NO-GO إلى أن يكتمل جرد البيانات وApp Check/Scheduler
  staging والتوقيع الحقيقي والنسخ الاحتياطي. لم يحدث deploy أو migration أو
  قراءة/كتابة `alrahmat-console`، ولم يحدث commit أو push.

## 2026-07-23 — حفظ MAJ-8 وBUG-002 في GitHub
- جُمعت تغييرات النظام المالي والحجوزات وإصلاح `BUG-002` والاختبارات وتقارير الإغلاق في commit واحد مترابط: `c0caad5 feat(finance): finalize MAJ-8 financial and booking workflows` (59 ملفًا).
- رُفع commit إلى `origin/main` دفعًا عاديًا، ثم طابقت نتيجة `git ls-remote` المعرّف الكامل المحلي `c0caad5d7e16a1506dd535beee35931ea353969c`.
- سبقت الحفظ بوابات خضراء: Flutter ‏46/46، Functions ‏17/17 على Node `v20.20.2`، Firebase Emulator ‏23/23 على `demo-financial-prestaging`، JSON ‏5/5، و`node --check` ‏10/10. بقيت 4 تحذيرات analyze قديمة دون أخطاء.
- فحص الأسرار للملفات المرفوعة: صفر تطابق عالي الثقة، وصفر معرّفات جهاز أو مسارات مستخدم محلية أو قيم IBAN. لم تُرفع إعدادات `.claude/settings.json` المحلية ولا قفل Gradle.
- لم يحدث deploy أو migration، ولم تُستخدم أو تُكتب بيئة `alrahmat-console` الإنتاجية.

## 2026-07-23 — إغلاق BUG-002 وMAJ-8
- فصل تقويم التوفر نهائيًا عن «حجوزاتي»: التقويم للعضو والضيف يستعمل callable المنقح `getBookingAvailability`، بينما بقيت حجوزات المستخدم على query مقيدة بـ`userId`.
- لم تتغير Firestore Rules؛ بقي list جميع حجوزات المجلس محظورًا على العضو. استجابة التوفر لا تعيد إلا `date` و`status`، واختبارات التنقيح والعزل تمنع تسرب الاسم أو الهاتف أو الملاحظات أو الإيصال أو الرسم المالي.
- أضيفت تغطية Flutter لحالات loading وerror وempty واليوم غير المتاح، وتغطية Emulator للعضو والضيف وعزل المجالس. النتائج: Flutter ‏46/46، Functions ‏17/17 على Node 20، Emulator ‏23/23، و4 تحذيرات analyze القديمة نفسها دون أخطاء جديدة.
- نجحت إعادة الاختبار على Samsung `SM-S948B` مرتين 2/2. ظهر تقويم العضو و«حجوزاتي» دون رسالة تعذر التحميل، وظهر يوم 6 أغسطس للحجز المعتمد كغير متاح.
- أُغلق `BUG-002` وأصبحت MAJ-8 جاهزة 100%. أُغلقت `TASK-004` بنسبة 100%. لم يحدث commit أو push أو deploy أو migration ولم يُستخدم `alrahmat-console`.

## 2026-07-23 — QA هاتف Samsung وشريط إشعارات Android لـMAJ-8
- بناء APK Debug مقيد صراحةً بـ`demo-financial-prestaging` وFirebase Emulator، والتحقق من صلاحيته (604 مدخلات ZIP، SHA-256: `2727F9156D52857629E6C6ECF8CC2CF67308BCBF760F51AEC41C8D09704A1234`) ثم تثبيته على Samsung `SM-S948B` دون deploy أو production.
- نجاح اختبار Android system tray بصريًا 4/4 عبر مستقبل QA المحلي الخاص بنسخة Debug: `5.000` و`7.500` و`8.000` و`12.500 ر.ع.`؛ لم تظهر كلمة «بيسة» ولا قيم البيسة الخام.
- نجاح QA البصري لشاشة رفع إيصال العضو: رسم الحجز `5.000`، رصيد السداد الجزئي `7.500`، والاشتراك `12.500` مع الرمز الرسمي وثلاث خانات عشرية. ونجاح شاشة رسم الضيف للحجز المعتمد بقيمة `7.500` وحالة `unpaid`.
- نجاح تقويم التوفر ومسار «المبلغ والدفع» للضيف، وظهور الحجز المعتمد للعضو. لكن تقويم التوفر للعضو فشل مرتين بسبب استخدام استعلام جميع حجوزات المجلس الذي ترفضه Rules للعضو؛ سُجل `BUG-002` ولم تُضعف القواعد.
- بقيت MAJ-8 غير مكتملة 100% بسبب `BUG-002`. بقيت `TASK-004` عند 98%، ولم يُنفذ commit أو push أو deploy أو migration، ولم يُستخدم `alrahmat-console`.

## 2026-07-22 — الإغلاق الفني الآلي لـMAJ-8 ودورة الحجوزات
- استكمال فحص الحالة المحلية فوق `72b3428` دون `pull` أو أوامر Git مدمرة، مع إبقاء جميع تعديلات المستخدم غير الملتزمة محفوظة وعدم تنفيذ commit أو push أو deploy أو migration.
- تأكيد أن سماكة شعار الريال مطبقة مركزيًا في `OmrSymbol` عبر `ui.ImageFilter.dilate` بنصف قطر متناسب ومحدود، دون تغيير `height` أو `BoxFit.contain` أو نسبة الأصل، ونجاح اختبارات Widget الخاصة به ضمن 40 اختبار Flutter.
- إضافة اختبار Firebase Emulator متصل لدورة حجز عضو كاملة: إنشاء العميل وفق Rules، رفض اعتماد غير المخول، اعتماد الحجز، إنشاء رسم واحد idempotent، رفع إيصال QA، رفضه، سداد جزئي، إكمال السداد، ثم إلغاء الحجز وتحويل الرسم المدفوع إلى `refundRequired` مع حفظ المبالغ.
- نجاح اختبار الضيف القائم بالتوازي: رسم غير المشترك، خصوصية الرسم، الرفع، الرفض/الاعتماد، السداد الجزئي والكامل ومنع الدفع الزائد.
- إصلاح أربع ملاحظات تحليل ميكانيكية لا تغير السلوك: non-null assertion زائد، import غير مستخدم، متغير محلي غير مستخدم، وأقواس `if`. بقيت 4 تحذيرات `unused_element` قديمة في `admin_dashboard.dart` ولم تُحذف كتل الواجهة تلقائيًا.
- البوابات النهائية: Flutter ‏40/40، Functions على Node 20 ‏17/17، Firebase Emulator على `demo-financial-prestaging` ‏23/23، JSON validator ‏5/5 مع 15 ملفًا صالحًا و3 placeholders legacy مستثناة، `node --check` ‏10/10، وفحص الأسرار 56 ملفًا و0 تطابق عالي الثقة.
- لم يتوفر هاتف متصل عبر ADB؛ بقي اختبار Android system tray وQA الهاتف الكامل يدويين. لذلك بقيت `TASK-004` قيد التنفيذ وارتفعت واقعيًا إلى 98%، ولم تُعلن MAJ-8 مكتملة 100%.

## 2026-07-19 — تعزيز شعار الريال (Bold) وتحليل معمارية الدفع الإلكتروني
- جعل شعار الريال العُماني الرسمي يظهر بوزن Bold في **جميع** مواضع العرض من المكوّن المركزي `OmrSymbol` فقط، عبر توسيع مورفولوجي (`ui.ImageFilter.dilate`) يزيد سماكة الحدود دون تكبير الحجم ولا تشويه الشكل ولا استبدال الرمز بـOMR. نصف قطر التوسعة يتناسب مع حجم الخط ليبقى الوزن متسقًا في كل الأحجام.
- تحديث `OmrAmount` وحقول الإدخال الموحّدة لترث الوزن الجديد تلقائيًا؛ الرقم يبقى بوزنه المعتاد والمحاذاة يسار الرمز محفوظة بلا Overflow أو اختلاف بين RTL وLTR.
- إضافة 3 اختبارات widget تتحقق من تطبيق التوسعة افتراضيًا وإمكان تعطيلها وبقاء الرقم مقروءًا؛ صورة واحدة فقط دون تكرار للرمز.
- إصلاح خطأ ترجمة سابق في `test/unattended_financial_security_regression_test.dart` (سلسلة غير خام كانت تفسّر `$rials`/`$fraction`) بجعلها Raw String، فعادت بوابة `flutter test` خضراء.
- توثيق قرار معماري: اعتماد الدفع الإلكتروني تلقائيًا بعد التحقق من Webhook دون اعتماد المدير المالي — **تحليل ومخطط فقط، لم يُفعَّل** التزامًا بـ`onlinePaymentsEnabled=false` والدستور (`DECISION_ELECTRONIC_PAYMENT_AUTO_APPROVAL_AR.md`).
- تأكيد أن بوابة JSON المتتبعة أصبحت خضراء عبر `scripts/validate-tracked-json.js` (15 ملفًا صالحًا + 3 placeholders فارغة مستثناة صراحةً).
- نتائج البوابات: `flutter test` 40/40، `npm test` 17/17، اختبار المدقق 5/5، `node --check` نظيف، `git diff --check` نظيف. `flutter analyze` أظهر 8 تحذيرات **سابقة** خارج نطاق المهمة (dead code في `admin_dashboard.dart` ومتغير غير مستخدم في `app_providers.dart`) لا علاقة لها بتعديل هذه الجلسة.

## 2026-07-16 — إغلاق QA للإيصالات وتوحيد عرض الريال العُماني
- تأكيد فتح PDF بصريًا من حساب المراجع داخل Emulator، ورفض إيصال مع ظهور السبب للعضو، ونجاح الدفع عن عضو واحد وإيصال موزع على عضوين مع اعتماد ذري وتدقيق وإشعارات.
- إضافة أصلَي PNG الرسميين لرمز الريال العُماني من صفحة البنك المركزي العُماني، مع مكوّن Flutter مركزي يضع الرمز الرسمي يسار القيمة ذات ثلاث خانات عشرية.
- توحيد عرض المبالغ في لوحات العضو والإدارة والمراجعة والحجوزات والإيصالات والإشعارات، مع بقاء التخزين والحساب أعدادًا صحيحة بالبيسة.
- دعم إدخال الريال بالأرقام العربية والإنجليزية والفواصل العربية والإنجليزية دون حسابات `double` جديدة.
- إضافة بيانات إشعار مالية منظمة (`amountBaisa` و`currencyCode=OMR` وقالب نص)، وتوافق خلفي للإشعارات القديمة، واستخدام `ر.ع.` فقط في نص إشعار Android النظامي الذي لا يدعم أصل PNG داخل النص.
- نتائج التحقق البرمجية الأولية: 32 اختبار Flutter و15 اختبار Functions و19 اختبار Emulator، و`flutter analyze` بلا مشكلات؛ يبقى توثيق التحقق البصري النهائي وبوابات التسليم النهائية في التقرير المخصص.

## 2026-07-16 — إصلاح فتح إيصال Emulator وتجهيز QA الهاتف
- إضافة استجابة bytes مقيدة خادميًا بـ`FUNCTIONS_EMULATOR` والمشروع `demo-financial-prestaging` مع إبقاء Signed URL للإنتاج.
- إضافة فحوص الحجم والنوع والاسم وتوقيع الملف، وفتح محلي typed عبر cache على Android.
- إظهار الرسوم المرتبطة بإيصال pending كـ«قيد المراجعة» وتعطيل إعادة اختيارها.
- توحيد Auth Emulator ضمن `firebase.json` وتوسيع seed الآمن ليشمل Storage وإيصال PDF deterministic.
- نجاح 13 اختبار Functions و18 اختبار Emulator و26 اختبار Flutter و`flutter analyze`.
- بناء APK Debug وتثبيته على Samsung دون مسح البيانات؛ بقي التحقق البصري متوقفًا بسبب قفل شاشة الهاتف، لذلك لم تبدأ مرحلة توحيد عملة الريال.

## 2026-07-15 — إغلاق نقاط المراجعة المالية على Node 20
- إثبات تشغيل Node `v20.20.2` في الطرفية وداخل Firebase Functions Emulator.
- إضافة pagination آمنة ومربوطة بنطاق المجلس والعضويات لـ`getPayableCharges`، مع جلب Flutter التلقائي لكل الصفحات واختبار يتجاوز أول 50 رسمًا.
- نقل قراءة إيصال المراجع إلى callable خادمية تعيد رابطًا قصير العمر بعد التحقق من العضوية الفعلية عبر `userId`، ومنع القراءة المباشرة بين المستخدمين في Storage Rules.
- إضافة financial notification outbox ذري وtrigger خادمي idempotent قابل لإعادة المحاولة، مع اختبار فشل ثم retry ومنع التكرار.
- نجاح `npm ci` و11 اختبار Functions و17 اختبار Emulator و22 اختبار Flutter؛ المجموع 50 اختبارًا ناجحًا و0 فاشل.
- إزالة `.claude/settings.local.json` من Git index مع إبقائه محليًا وignored.
- بقيت المهمة المالية `in_progress` وارتفعت واقعيًا إلى 95%؛ المتبقي QA الأجهزة وstaging وApp Check ومسار رسوم المناسبات.

## 2026-07-15 — النظام المالي والاشتراكات (المرحلة الأولى)
- إضافة إعدادات مالية مستقلة لكل مجلس وباقات وحسابات أعضاء ورسوم بالبيسة.
- إضافة Dashboard عربية لملخص الحساب والرسوم وحالات السداد.
- إضافة دفع جماعي بإيصال واحد عن النفس أو الآخرين أو كليهما مع تحقق خادمي ذري.
- إضافة البحث العربي المحدود عبر member_directory ومزامنته خادميًا.
- إضافة مراجعة الإيصالات والإشعارات وسجل التدقيق ومولد الاشتراكات والمتأخرات.
- إضافة قواعد وفهارس Firebase وسكربت ترحيل idempotent يعمل بوضع dry-run افتراضي.
- حالة التحقق: flutter analyze ناجح، flutter test ناجح (22)، واختبارات Node ناجحة (3)، وترجمة قواعد Firestore بالمحاكي ناجحة.
- المتبقي: اختبارات تكامل كاملة للدوال والقواعد، QA على الأجهزة، مراجعة dry-run ببيانات منسوخة قبل النشر.

## 2026-07-11
- تم إنشاء قاعدة المعرفة Project Brain.
- تم إنشاء نظام متابعة المهام ونسبة الإنجاز.
- تم تجهيز مزامنة النسخة الاحتياطية مع OneDrive.

## 2026-07-11 19:50 â€” Create Project Brain
- Ø§Ù„Ø­Ø§Ù„Ø©: **Tested**
- Ø§Ù„Ù…Ù„Ø®Øµ: Created project tracking and OneDrive synchronization.
- Ø§Ù„Ù…Ù„ÙØ§Øª Ø§Ù„Ù…ØªØ£Ø«Ø±Ø©: project-brain, scripts
- Ø§Ù„Ø§Ø®ØªØ¨Ø§Ø±Ø§Øª: Dashboard update and OneDrive sync
- Ø§Ù„Ø®Ø·ÙˆØ§Øª Ø§Ù„Ù‚Ø§Ø¯Ù…Ø©: Commit and push to GitHub
