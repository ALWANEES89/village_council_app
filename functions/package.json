{
  "name": "village-council-functions",
  "description": "Cloud Functions for village council app",
  "scripts": {
    "build": "echo 'no build step'",
    "deploy": "firebase deploy --only functions"
  },
  "engines": { "node": "20" },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
