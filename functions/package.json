{
  "name": "village-council-functions",
  "description": "Cloud Functions for village council app",
  "scripts": {
    "build": "echo 'no build step'",
    "test": "node --test financial.test.js",
    "test:emulator": "node --test financial_emulator.test.js",
    "deploy": "firebase deploy --only functions"
  },
  "engines": { "node": "20" },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
